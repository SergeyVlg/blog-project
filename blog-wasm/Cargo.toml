[package]
name = "blog-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dioxus = { version = "0.7.5", features = ["web"] }
gloo-net = { version = "0.6.0", features = ["http"] }
blog-client = { path = "../blog-client", default-features = false }
